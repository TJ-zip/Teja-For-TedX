# One npm script name per line. Only names already declared in package.json
# are accepted; anything else is rejected by the workflow.
#
# Requested for: fix/eslint-flat-config — verify the FlatCompat removal fixes
# the "Converting circular structure to JSON" lint crash.
lint
typecheck
build
